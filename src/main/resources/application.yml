################## Application Configuration ####################
---
spring:
  config:
    activate:
      on-profile: peer-1
    import: "optional:configserver:http://localhost:8091"
  cloud:
    config:
      name: peer,eureka-server,cloud-bus
      profile: local

server:
  port: ${PORT:3001}
---
spring:
  config:
    activate:
      on-profile: peer-2
    import: "optional:configserver:http://localhost:8091"
  cloud:
    config:
      name: peer,eureka-server,cloud-bus
      profile: local

server:
  port: ${PORT:3002}
---
spring:
  config:
    activate:
      on-profile: peer-3
    import: "optional:configserver:http://localhost:8091"
  cloud:
    config:
      name: peer,eureka-server,cloud-bus
      profile: local

server:
  port: ${PORT:3003}